{
  "name": "game-dev",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 6100",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 6100"
  },
  "dependencies": {
    "phaser": "^3.85.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
